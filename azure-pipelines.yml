# Node.js with React
# Build a Node.js project that uses React.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript
trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  - group: MyVariables
  - name: NODE_VERSION
    value: '22.x'
  - name: SONAR_ORG
    value: 'jimmyhyusein' # ЗАМЕНИ С ТВОЯ ORG KEY
  - name: SONAR_PROJECT
    value: 'JimmyHyusein_TodoApp' # ЗАМЕНИ С ТВОЯ PROJECT KEY

steps:
# 1. Инсталация на Node.js
- task: UseNode@1
  displayName: 'Install Node.js'
  inputs:
    version: $(NODE_VERSION)

# 2. ПОДГОТОВКА НА SONARCLOUD (ЗАДЪЛЖИТЕЛНО В НАЧАЛОТО)
- task: SonarCloudPrepare@3
  displayName: 'Prepare SonarCloud Analysis'
  inputs:
    SonarCloud: 'SonarCloudConnection'
    organization: $(SONAR_ORG)
    scannerMode: 'CLI'
    configMode: 'manual'
    cliProjectKey: $(SONAR_PROJECT)
    extraProperties: |
      sonar.javascript.lcov.reportPaths=**/coverage/lcov.info
      sonar.sources=.
      sonar.exclusions=**/node_modules/**,**/*.test.js

# 3. ФУНКЦИОНАЛНИ ТЕСТОВЕ: SERVER
- script: |
    cd server
    npm install
    npm test
  displayName: 'Functional Tests: Server'

# 4. ФУНКЦИОНАЛНИ ТЕСТОВЕ: CLIENT
- script: |
    cd client
    npm install
    npm test
  displayName: 'Functional Tests: Client'
  env:
    CI: true

# 5. ПУБЛИКУВАНЕ НА РЕЗУЛТАТИ ОТ ТЕСТОВЕТЕ В AZURE
- task: PublishTestResults@2
  displayName: 'Publish Test Results'
  condition: succeededOrFailed()
  inputs:
    testResultsFormat: 'JUnit'
    testResultsFiles: '**/test-results/*.xml'

# 6. ИЗПЪЛНЕНИЕ НА SONARCLOUD АНАЛИЗ (СЛЕД ТЕСТОВЕТЕ)
- task: SonarCloudAnalyze@3
  displayName: 'Run SonarCloud Analysis'

- task: SonarCloudPublish@3
  displayName: 'Publish Quality Gate Result'
  inputs:
    pollingTimeoutSec: '300'

# 7. НЕФУНКЦИОНАЛНИ ТЕСТОВЕ: SECURITY SCAN
- script: |
    cd server
    npm audit --audit-level=high
  displayName: 'Security Scan (Server)'
  continueOnError: true 

# 8. BUILD НА КЛИЕНТСКИЯ ПРОЕКТ
- script: |
    cd client
    npm run build
  displayName: 'Build Client'

# 9. СЪЗДАВАНЕ И ПУБЛИКУВАНЕ НА ARTIFACTS
- task: CopyFiles@2
  displayName: 'Copy Build to Staging'
  inputs:
    SourceFolder: 'client/build'
    Contents: '**'
    TargetFolder: '$(Build.ArtifactStagingDirectory)'

- task: PublishBuildArtifacts@1
  displayName: 'Publish Artifact: drop'
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'
    publishLocation: 'Container'

# 10. DOCKER АВТОМАТИЗАЦИЯ (САМО АКО ВСИЧКО ПРЕДИ ТОВА Е УСПЕШНО)
- task: Docker@2
  displayName: 'Build and Push Docker Image'
  condition: succeeded()
  inputs:
    containerRegistry: 'MyDockerHub'  
    repository: 'nhyusein/todo-app'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: |
      $(Build.BuildId)
      latest